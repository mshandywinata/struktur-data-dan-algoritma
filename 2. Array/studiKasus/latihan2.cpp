#include <iostream>
using namespace std;

int main() {
    int scores[10] = {97, 90, 68, 75, 80, 89, 62, 45, 99, 71};
    int lenScore = sizeof(scores) / sizeof(int);
    int total;

    for (int i = 0; i < lenScore; i++) {
        // return 0;
    }

    return 0;
}